# Core framework
Django==4.2.25

# WebSocket support for Django (ASGI-compatible)
channels==4.1.0
daphne==4.1.2

# rest framework
rest_framework==3.16.1

# cors cross origin
django-cors-headers==4.9.0